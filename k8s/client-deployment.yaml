apiVersion: apps/v1
kind: Deployment
metadata:
  name: client-deplopyment
spec:
  replicas: 3
  selector:
    matchLabels:
      component: web
  template:
    metadata:
      labels:
        component: web
    spec:
      containers:
        - name: client
          image: testep02/multi-client
          ports:
            - containerPort: 3000
